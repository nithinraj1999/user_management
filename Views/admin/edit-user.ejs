<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="w-25 p-3   d-flex align-item-center justify-content-center mx-auto my-auto box">
        <form method="post" action="edit-user">
          <div>
            <h2>Update User</h2>
          </div>
          <div class="mb-3">
            <input type="text" class="form-control inp" required placeholder="Full Name" value="<%=users.name %>" pattern="^\S(.*\S)?$"  name="name">
          </div>
          <div class="mb-3">
            <input type="text" class="form-control inp" required placeholder="phone" value="<%= users.phone %>" pattern="^\S(.*\S)?$"  name="phone">
          </div>
          <div class="mb-3">
            <input type="email" class="form-control inp" required id="exampleInputEmail1" value="<%= users.email %>" pattern="^\S(.*\S)?$"   aria-describedby="emailHelp"
              placeholder="email" name="email">
          </div>
          <div class="mb-3">
            <input type="hidden" class="form-control inp" required value="<%= users._id %>" pattern="^\S(.*\S)?$"   aria-describedby="emailHelp" name="id">
          </div>
          
          <button type="submit" class="btn btn-dark">Update</button>
          <div>
            <% if(typeof msg !="undefined"){ %>
              <h4><%= msg %></h4>
              <%} %>
          </div>
         
            
            
          </div>
        </form>
      </div>
</body>
</html>